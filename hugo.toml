baseURL = "https://voided-name.github.io/FNPSO/"
defaultContentLanguage = "en"
# Automatically generate robots.txt
enableRobotsTXT = true
languageCode = "en"
theme = "gokarna"
title = "FNPSO"
timeZone = "Local"

[params]
  avatarURL = "https://voided-name.github.io/FNPSO/images/fnpso.jpg"
  avatarSize = "size-xl"
  description = "For the wellfare of the students"
  footer = "Federation of Non-Political Student Organization"
  socialIcons = [
        {name = "facebook", url = "https://www.facebook.com/FNPSO"},
  ]

[menu]
  [[menu.main]]
    # Display name
    name = "Posts"

    # Relative URL slug (appended to baseURL)
    url = "/posts/"

    # Lower weights are listed first in the menu (leftmost); higher weights are
    # listed last in the menu (rightmost)
    weight = 1

  [[menu.main]]
    name = "Tags"
    url = "/tags/"
    weight = 2

  [[menu.main]]
    name = "Resources"
    url = "/templates/"

    weight = 3
    
# [[menu.main]]
    # Unique identifiers are required for menu entries without a name property,
    # or for menu entries which re-use a name
# identifier = "facebook"

    # Absolute URL to external resource
# url = "https://www.facebook.com/FNPSO"
# weight = 4
    
    # Surround the menu entry (or name) with HTML content, such as Feather
    # icons: https://feathericons.com
# pre = "<span data-feather='facebook'></span>"
# post = ""

